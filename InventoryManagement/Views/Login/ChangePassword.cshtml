@model InventoryManagement.Entity.Common.ChangePassword
    @using InventoryManagement.Common
@using InventoryManagement.Entity.Common
    @{ 
        string CurrentPasswordValue = (Session["LoginUser"] as User).Password;
        
    }
<style>
    .ui-dialog-content-Change{
        height:235px!important;
    }
</style>
<script type="text/javascript">
    $(document).ready(function () {
        $(".preloader").hide();
        @*$("form[name=changePasswordForm]").unbind("submit");
        $("form[name=changePasswordForm]").bind('submit', function (e) {
            e.preventDefault();
            e.stopPropagation();
            var formdata = new FormData($(this)[0]);
            if (ValidatePassword() == true) {
                $.ajax({
                    url: '@Url.Action("ChangePassword", "Login")',
                    type: 'POST',
                    data: formdata,
                    processData: false,
                    contentType: false,
                    dataType: "json",
                    success: function (objResponse) {
                        if (objResponse != null) {
                            if (objResponse.ResponseStatus == "FAILED") {
                                OpenDialog("dialogMessage", objResponse.ResponseMessage, "false");
                                reset();
                            }
                            else {
                                $("#dialogResetPassword").dialog("close");
                            }
                        }
                    },
                    error: function (xhr, data) {
                        //  return false;
                        console.log(xhr);
                        console.log("Error:", data);
                    }
                });
            }
        });*@
        
        
    });
    
</script>
    <div class="col-lg-12">
       
        
        <div id="dialogWarning" title="Reset Password">

        </div>
        <form onsubmit="false" data-parsley-validate class="validateForm form-horizontal form-label-left" id="changePasswordForm" name="changePasswordForm">
            @Html.HiddenFor(m => m.PartyCode,new {id="hiddenPartyCode",name= "hiddenPartyCode" })
            @Html.HiddenFor(m => m.UserName, new { id = "hiddenUserName", name = "hiddenUserName" })
            <div class="form-group">
                <label>Current Password</label>
                <div class="col-md-10">
                    @Html.TextBoxFor(model => model.CurrentPassword, new {type="Password", id = "CurrentPassword", name = "CurrentPassword", @required = "required", @class = "form-control" })

                </div>
            </div>

            <div class="form-group">
                <label>New Password</label>
                <div class="col-md-10">
                    @Html.TextBoxFor(model => model.NewPassword, new { type = "Password", id = "NewPassword", name = "NewPassword", @class = "form-control", @required = "required" })

                </div>
            </div>
            <div class="form-group">
                <label>Confirm Password</label>
                <div class="col-md-10">
                    @Html.TextBoxFor(model => model.ConfirmPassword, new { type = "Password", id = "ConfirmPassword", name = "ConfirmPassword", @class = "form-control", @required = "required" })

                </div>
            </div>



            @*<div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Change" class="btn btn-default" />
                </div>
            </div>*@
        </form>
    </div>


